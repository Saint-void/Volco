# Core audio, wake word, and voice activity detection
numpy
openwakeword
onnxruntime
PyAudio
webrtcvad

# Network, WebRTC, and Spotify control
aiohttp
aiortc
requests
zeroconf

# Raspberry Pi hardware and Bluetooth data pipe
bluezero; platform_system == "Linux"
gpiozero; platform_system == "Linux"
pyserial

# Optional keyboard fallback used during development/testing
keyboard; platform_system == "Windows"
